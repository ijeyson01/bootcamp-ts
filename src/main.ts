import { MiClase } from "./metodos/01-metodos";

let variableClase: MiClase = new MiClase();

// mostrar todos los objetos del array persona en formato JSON
//variableClase.mostrarValoresArrayPersona();

// mostrar una persona por ID
//variableClase.buscarPersonaById(1);

// mostrar personas por rango de edad
//variableClase.buscarPersonaByAge(34, 40);

// mostrar personas por nombre
variableClase.buscarPorNombre('m');